export class CommonConstants {
  // Auth provider name in the localStorage
  static providerKeyName = "provider";
  static whenUserWasNotifiedAboutPopupKeyName = "userWasAlreadyNotifiedAboutPopupOn";
  static changeHtmlImageBackInMilliseconds = 3000;
  static holdToastWarningForMilliseconds = 20000;
}
